import React from 'react';
import { WeatherDay } from './WeatherDay';

export const WeatherDays = () => {
    return (
        <div>
            <h1>WeatherDays</h1>
            <WeatherDay />
        </div>
    );
};
